# Web Framework
fastapi==0.115.12
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database (async PostgreSQL + migrations)
sqlalchemy[asyncio]==2.0.40
asyncpg==0.30.0
alembic==1.15.2

# Validation
pydantic==2.11.3
pydantic-settings==2.9.1
email-validator==2.2.0

# Auth — PyJWT replaces abandoned python-jose (CVE-2024-33664)
#       — bcrypt directly replaces abandoned passlib
PyJWT[crypto]==2.11.0
bcrypt==4.3.0

# Cache & Queues
redis[hiredis]==5.3.0
celery[redis]==5.5.2

# HTTP Client
httpx==0.28.1

# Logging (structured)
structlog==25.4.0

# Testing
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.1

# Utils
python-dateutil==2.9.0.post0
orjson==3.10.18

# Real-time (Socket.io for Python)
python-socketio[asyncio]==5.12.1

# Task Queue alternative to Celery (lighter, async-native)
arq==0.26.1

# Browser automation (HBook scraper — replaces Puppeteer)
playwright==1.50.0
