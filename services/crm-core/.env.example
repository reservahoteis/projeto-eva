# =============================================================================
# CRM Core - Environment Configuration Example
# Copy this file to .env and fill in real values before running.
# NEVER commit a .env file containing real secrets to version control.
# =============================================================================

# ---------------------------------------------------------------------------
# Database
# Format: postgresql+asyncpg://<user>:<password>@<host>:<port>/<db>
# asyncpg is the async PostgreSQL driver required by SQLAlchemy[asyncio].
# For production, use a connection pooler (PgBouncer) in front of Postgres.
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/crm_dev

# Echo every SQL statement to stdout (useful for debugging N+1 queries).
# Set to false in production to avoid log noise and latency.
DATABASE_ECHO=false

# ---------------------------------------------------------------------------
# Redis
# Used by: BullMQ-style Celery queues, response cache, session store.
# DB index 0 is reserved for crm-core. Other services should use /1, /2, ...
# ---------------------------------------------------------------------------
REDIS_URL=redis://redis:6379/0

# ---------------------------------------------------------------------------
# Authentication
# JWT_SECRET must be a random string of at least 32 characters.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
# Must be the SAME value across all services that validate JWT tokens.
# ---------------------------------------------------------------------------
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
DEBUG=true
APP_NAME=CRM Core
APP_VERSION=0.1.0

# Comma-separated list of allowed CORS origins.
# In development, localhost:3000 is the Next.js dev server.
CORS_ORIGINS=http://localhost:3000,https://hoteisreserva.com.br
