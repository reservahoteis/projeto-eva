# ============================================
# Dev Backend Upstream (Instagram/Messenger)
# Routes Instagram and Messenger webhooks to dev container
# ============================================
upstream backend_dev {
    server backend-dev:3002;
    keepalive 16;
    keepalive_timeout 60s;
}
